# Models

## Purpose

AI model weight storage. Files are NOT committed to Git.

## Downloading Models

`ash
python scripts/models/download_models.py
`\n
## Model Registry

See registry.yaml for model versions, download URLs, and SHA-256 checksums.
